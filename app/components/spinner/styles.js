const React = require("react-native");

const { StyleSheet } = React;

export default {
  view: {
    flex: 1,
    flexDirection: 'column',
    justifyContent: 'center',
    alignItems: 'center',
    backgroundColor: "#FFF",
  }
};
